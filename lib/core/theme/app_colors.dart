import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF6C5CE7);
  static const secondary = Color(0xFFFFD700);

  static const success = Color(0xFF2ECC71);
  static const danger = Color(0xFFE74C3C);
  static const warning = Color(0xFFF1C40F);

  static const lightBg = Colors.white;
  static const darkBg = Color(0xFF121212);

  static const textLight = Colors.black;
  static const textDark = Colors.white;

  static const mortal = Color(0xFF95A5A6);      // Cinza
  static const guerreiro = Color(0xFF27AE60);   // Verde
  static const elite = Color(0xFF2980B9);       // Azul
  static const mestre = Color(0xFF8E44AD);      // Roxo
  static const lenda = Color(0xFFFFD700);       // Dourado
  static const titan = Color(0xFFFF8C00);       // Laranja
  static const aura = Color(0xFF00D2FF);        // Azul neon
  static const chaos = Color(0xFFEB02F7);       // Rosa/Roxo neon

  static const cardLight = Color(0xFFF8F9FA);
  static const cardDark = Color(0xFF1E1E1E);

  static const borderLight = Color(0xFFE0E0E0);
  static const borderDark = Color(0xFF2D2D2D);
}